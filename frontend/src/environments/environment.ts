// Local dev — backend on localhost:4000.
// Production overrides come from environment.prod.ts (see angular.json
// fileReplacements under the production configuration).
export const environment = {
  production: false,
  apiUrl: "http://localhost:4000",
};
