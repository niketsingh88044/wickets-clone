// Production — frontend built and served from GitHub Pages.
// Change apiUrl below to your deployed backend (e.g. Render / Fly / Railway)
// and rebuild. The placeholder will produce visible network errors so you
// know it hasn't been swapped yet.
export const environment = {
  production: true,
  apiUrl: "https://REPLACE-ME-WITH-YOUR-BACKEND-URL.example.com",
};
